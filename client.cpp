#include <string>
#include <vector>

using namespace std;
class Client
{
public:
    Client(string name_in)
    {
        if (true)
        {
        }
    }

private:
    int id;
    string name;
    vector<int> following;
};